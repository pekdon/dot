XTerm*metaSendsEscape: true
XTerm*loginShell: true
XTerm*saveLines: 10000
XTerm*termName: xterm-256color
XTerm*allowedXtermControlOps: 4,10,11,50

XLock*mode: blank

dt.obsolete: true

! used by pekwm to select keyboard layouts
pekwm*keyboardLayouts: us se
pekwm.audio.player: cmus
pekwm.audio.control: pulse
